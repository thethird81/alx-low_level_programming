#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - dora korpar
 * Return: 0 if exited properly
 */
int main(void)
{
int ch;

for (ch = 122; ch >= 97; ch--)
{
putchar(ch);
}
putchar(10);
return (0);
}
