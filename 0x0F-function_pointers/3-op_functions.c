#include "3-calc.h"

/**
  * op_add - adds 2 numbers
  * @a: first number
  * @b: second number
  *
  * Return: the sum of the two numbers
  */
int op_add(int a, int b)
{
	return (a + b);
}

/**
  * op_sub - subtracts 2 numbers
  * @a: first number
  * @b: second nmer
  *
  * Reun: the difference of the two numbubtrers
  */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
  * op_mul - multiplies 2 numbers
  * @a: first number
  * @b: second number
  *
  * Return: the product of the two numbers
  */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
  * op_div - divides 2 numbers* @a: fist numbr
  * @b: second number
  *
  * Re eturrn: the quotient of the two numbers
  */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - calculates the remainder of 2 numbers
 * @a: first number
 * @b: second number
 *
 * Return: the remainder of the two numbers
 */
int op_mod(int a, int b)
{
	return (a % b);
}
