#include "main.h"



/**
 * _isdigit - checks cas
 * @c: - character
 * Return: 0 if caps 1 if lower case
 */



int  _isdigit(int c)

{

	if ((c >= 48) && (c <= 57))
	return (1);
	else
	return (0);
}
